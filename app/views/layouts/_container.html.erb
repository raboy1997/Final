<div class="container">
  <div class="row">
    <div class="col-md-8">
      <%= yield %>
    </div>
    <div class="col-md-4">
      <ul class="list-group">

          <%= link_to "Всі Теми", root_path, class: "list-group-item" %>

        <% Category.all.each do |category| %>

              <%= link_to category.name, articles_path(category: category.name), class: "list-group-item" %>

        <% end %>
      </ul>
    </div>
  </div>
</div>
